<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
	xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    prism:ViewModelLocator.AutowireViewModel="True"
	xmlns:local="clr-namespace:Sweeper.Views;assembly=Sweeper"
    x:Class="Sweeper.Views.AboutPage"
    Title="About">



	  <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
        <StackLayout Orientation="Horizontal">
            <Label Text="Package" VerticalOptions="Center" HorizontalOptions="Center" />
            <Label Text="{Binding AboutModel.AppInfo.PackageName}" VerticalOptions="Center" HorizontalOptions="Center" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Name" VerticalOptions="Center" HorizontalOptions="Center" />
            <Label Text="{Binding AboutModel.AppInfo.Name}" VerticalOptions="Center" HorizontalOptions="Center" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Version" VerticalOptions="Center" HorizontalOptions="Center" />
            <Label Text="{Binding AboutModel.AppInfo.Version}" VerticalOptions="Center" HorizontalOptions="Center" />
        </StackLayout>

    </StackLayout>
    

</ContentPage>

